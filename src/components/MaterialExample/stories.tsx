import { Story, Meta } from '@storybook/react'
import MaterialExample from '.'

export default {
  title: 'MaterialExample',
  component: MaterialExample
} as Meta

export const Default: Story = () => <MaterialExample />
